This text file only exists so that the folder containing this file doesn't get skipped over when pushing to git.

After adding a platform, the three affected directories (merges, platforms, plugins) contain files and this readme can be deleted.
